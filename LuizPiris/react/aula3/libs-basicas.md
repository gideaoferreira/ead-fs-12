# libs

# instalação padrão:

- npm install bootstrap

# instalação com "install" abreviado:

- npm i @popperjs/core

# instalação de várias libs com um comando só:

- npm i react-router-dom sass ( -D ou --save-dev)       # para com instalar com modo developer
